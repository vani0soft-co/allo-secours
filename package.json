{
    "name": "allo-secours",
    "version": "1.0.0",
    "description": "Application mobile pour la géolocalisation des services de santé",
    "private": true,
    "scripts": {
        "backend:start": "cd backend && npm run dev",
        "backend:build": "cd backend && npm run build",
        "backend:test": "cd backend && npm test",
        "backend:seed": "cd backend && npm run seed",
        "backend:install": "cd backend && npm install",
        "frontend:get": "flutter pub get",
        "frontend:analyze": "flutter analyze",
        "frontend:test": "flutter test",
        "frontend:build:apk": "flutter build apk --release",
        "frontend:build:ios": "flutter build ios --release",
        "frontend:run": "flutter run",
        "docker:up": "docker-compose up -d",
        "docker:down": "docker-compose down",
        "docker:logs": "docker-compose logs -f",
        "start": "npm run docker:up && npm run backend:seed",
        "dev": "npm run docker:up && npm run backend:start",
        "build": "npm run frontend:build:apk && npm run backend:build",
        "test": "npm run backend:test && npm run frontend:test",
        "lint": "npm run backend:lint && npm run frontend:analyze",
        "seed": "npm run backend:seed",
        "update-all": "npm run backend:install && npm run frontend:get"
    },
    "keywords": [
        "flutter",
        "dart",
        "health",
        "geolocation",
        "hospitals",
        "pharmacies"
    ],
    "author": "Votre Nom",
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^20.0.0"
    }
}